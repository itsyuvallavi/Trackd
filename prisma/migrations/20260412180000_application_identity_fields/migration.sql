-- Application identity columns are created by `20260411100000_bot_baseline_tables`.
-- Keeping this migration file preserves ordering and `_prisma_migrations` history for databases
-- that already applied the older ALTER-only version.
SELECT 1;
